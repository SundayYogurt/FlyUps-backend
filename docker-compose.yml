services:
  postgres:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: flyup
    ports:
      - "5433:5432"
    volumes:

      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
