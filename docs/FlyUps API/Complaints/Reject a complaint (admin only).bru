meta {
  name: Reject a complaint (admin only)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/admin/complaints/:id/reject
  body: none
  auth: inherit
}

params:path {
  id: 
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/admin/complaints/:id/reject
    method: PATCH
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
