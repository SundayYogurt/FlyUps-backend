meta {
  name: Stripe webhook
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/stripe/webhook
  body: none
  auth: inherit
}

headers {
  Stripe-Signature: 
}

example {
  name: 200 Response
  description: webhook received
  
  request: {
    url: {{baseUrl}}/stripe/webhook
    method: POST
    mode: none
    headers: {
      Stripe-Signature: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

example {
  name: 400 Response
  description: invalid signature or payload
  
  request: {
    url: {{baseUrl}}/stripe/webhook
    method: POST
    mode: none
    headers: {
      Stripe-Signature: 
    }
  }
  
  response: {
    status: {
      code: 400
      text: Bad Request
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
