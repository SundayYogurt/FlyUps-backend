meta {
  name: Resolve Complaint
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/admin/complaints/1/resolve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "resolution": "Issue has been reviewed and resolved"
  }
}
