meta {
  name: Create Category
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "name": "Technology",
    "description": "Tech projects"
  }
}
