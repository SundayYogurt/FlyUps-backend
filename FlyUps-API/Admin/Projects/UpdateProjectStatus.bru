meta {
  name: Update Project Status
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/admin/projects/1/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "status": "active"
  }
}
