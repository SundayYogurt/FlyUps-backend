meta {
  name: Reset Password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/reset-password?reset_token=YOUR_RESET_TOKEN
  body: json
  auth: none
}

body:json {
  {
    "new_password": "newpassword123"
  }
}
