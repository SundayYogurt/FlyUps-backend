meta {
  name: Upload File
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/upload
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file(/path/to/your/file.jpg)
}
