meta {
  name: Sign Out
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/user/signout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
