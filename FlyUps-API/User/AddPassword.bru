meta {
  name: Add Password
  type: http
  seq: 10
}

put {
  url: {{baseUrl}}/user/add-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "password": "newpassword123",
    "confirm_password": "newpassword123"
  }
}
